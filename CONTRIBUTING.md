# 贡献说明

**Issue：欢迎提。** 仓库的 Issues **已开启** —— 这是本项目**唯一**的反馈入口。

**Pull Request：暂不直接合并。** 这不是针对谁，而是能力边界：本项目由个人在业余时间维护，
没有精力承担 PR 所需的评审、与你设计意图的对齐、以及为合并后的行为负责。
**这是对我自己时间与精力边界的说明，不是对你改动价值的评价。**

## 你可以做什么

以下都**不需要**经过我同意（MIT 许可已授权，见 [LICENSE](LICENSE)）：

| 你想做的 | 怎么做 |
|---|---|
| 改代码自己用 | **Fork** 一份，随便改，不必知会 |
| 打包给朋友 | 可以。MIT 只要求保留版权声明与许可文本 |
| 学实现思路 | 随便看。代码里有大量注释解释「为什么这样做」 |
| 自己构建 | 见 README「快速开始」与构建脚本。⚠️ `jniLibs/*.so`（第三方二进制，约 100MB）**不入库**，首次构建需先跑 `node rp-workspace/scripts/fetch-native-libs.mjs` 获取（带 SHA256 校验） |
| 发现 bug | **提 Issue**（欢迎）。先看 README 的「兼容分级」——若属 **Tier 3（明确不承诺）**，那是预期内的不兼容，不算 bug |
| 想当共同开发者 | **提 Issue 说明你打算做什么**；由我指定后把你加为 **collaborator**（直接提交权限），届时你的改动可直接进主干 |

## 请不要做

- ❌ **提 PR 后等合并** —— 本仓库不直接合并外部 PR（见上）。想合回来请走 Issue 说明改动，
  我会**自己重新实现一遍**（这样我能对合并后的每一行负责）
- ❌ **发邮件/私信催修** —— 没有响应时限承诺
- ❌ **在别处替我承诺** —— 本项目不提供任何支持保证

## 如果你确实改出了有价值的东西

Fork 后自行发布即可（MIT 允许）。**不需要**征求同意，也**不必**回馈上游——
如果你希望我知道，可以**提 Issue** 或在你的仓库里提一句；但我不保证会及时回应或合并。

## 这条规则会变吗

可能会。如果将来有精力做社区协作，我会更新本文件并在 README 同步说明。
在此之前的沉默**不代表**任何态度，只是时间与精力的现实约束。