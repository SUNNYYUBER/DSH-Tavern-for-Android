# ===== vendor / 上游 runtime（scripts/build-dsht.ps1 可完整重建，不入库）=====
node_modules/
deepseek-harness/
dsh-runtime/
dsh-runtime-android/
dsh-runtime-x64/
dsh-runtime-src/
downloads/
pc-verify-home/

# ===== 构建产物 / 交付物 =====
*.apk
*.aab
dist/
_pending-deploy/
**/android/app/build/
**/android/build/
**/android/.gradle/
**/android/local.properties
**/android/app/src/main/assets/dsh-runtime.zip
**/android/captures/
**/.kotlin/

# ===== 签名文件（严禁入库）=====
*.keystore
*.jks
**/keystore.properties

# ===== 大体积测试输入（不入库，本机保留）=====
test-data-zip/

# ===== 已归档的一次性探针/审计暂存（2026-09-08 移至 D:\DSH-RolePlay-archive）=====
.audit/
tmp/

# ===== 编辑器 / 系统 =====
.trae/
.vscode/
.idea/
*.log
# 门禁真值日志例外（独立目录，不动 tmp/ 的忽略规则——doc-refs 判据⑤ 的负控前提）：
# audit-baseline-claims 的「上次构建实测」真值来源（CI 与本地必须用同一份）。发版即刷新。
!rp-workspace/truth-logs/*.log
Thumbs.db
Desktop.ini
.DS_Store

# 升级回滚备份（大文件，不进版本控制）
backup/

# ===== 0.1.5 升级阶段 3 设备备份（921MB，本机保留不入库；T-01 2026-09-10）=====
stage3-device/

# ===== 原始抓包（含真实 RP 正文/人名，严禁入库；T-25 大扫除范畴）=====
# TT 侧采样参数抓包：保留在 golden/tt-sampling/ 本机对照，只把脱敏结论写进
# docs/DSHT-VS-TT-DIFF-2026-09-10.md §D-7。
golden/tt-sampling/

# 【E4 修复 2026-09-13】同类内容规则不一致修复：
# golden/dsht（DSHT 请求体抓包）与 golden/st（ST 抓包 dump）与上面的 tt-sampling
# **同一性质**（均含真实 RP 正文/卡名，PAYLOAD 8 项命中全在此），却未忽略。
# 现对齐：整目录出库，本机保留供 golden-*.mjs 对照使用。
golden/dsht/
golden/st/

# 【E4 新增 2026-09-13】以下目录含真实卡名/人设名/本机路径，属 T-25 大扫除范畴：
# · .workbuddy/ —— AI 会话记忆日记（含词表命中 + C:\Users\ 路径）
# · .goal/      —— 升级工作区（GOAL.md 等含词表命中）
.workbuddy/
.goal/

# 【E4 新增 2026-09-13】测试/编译运行产物（可再生）
rp-workspace/packages/.vitest/
*.tsbuildinfo

# 【T-25b 2026-09-19】发布前清理补漏（盲区收口）：
# · __pycache__/ —— Python 编译缓存（曾有 2 个 .pyc 误入库）
# · rp-workspace/artifacts/ —— 设备验证截图（**截图里是真实会话内容**：
#   文件名 native-conversation-migrated-history 即明示展示迁移后的聊天记录）。
#   ⚠️ 这一类是二进制，**发布卫生扫描（只扫文本）结构上看不到它** ⇒ 必须靠本条守住。
__pycache__/
rp-workspace/artifacts/

# 【E3 2026-09-13】AI 对话归档（2.11MB，含 15 处 wxid + 大量本机路径）
# 移到 docs-archive/ 保留原件但出库（不销毁；项目唯一完整历史档案）。
# 说明：docs-archive/ 与 docs/ 是两个不同目录——前者专放「保留但不出库」的档案。
docs-archive/

# 【E2 2026-09-13】历史归档文档出库（与 Plan 文档同性质：含本机路径/真实卡名/
# 第三方 provider 域名等 T-25 范畴内容；本机保留供追溯，不进任何未来提交）。
# · docs/archive/    —— 早期计划文档（PROJECT_PLAN / IMPORT_REWORK_PLAN / UPDATE-SOP 等）
# · loop-mvu-interact/ —— MVU 联动验证工作区（GOAL/LEARNINGS/verify-report 等）
# ⚠️ loop-mvu-interact 被 golden-verify*.mjs 用**相对路径**引用 —— 出库后文件仍在
#    本机，脚本照常可跑；但换机器 clone 后这些脚本会缺输入，届时需另行提供样本。
docs/archive/
loop-mvu-interact/

# ===== 发布卫生扫描的词表（含真实卡名/人设名，严禁入库；T-25）=====
rp-workspace/scripts/publish-hygiene-words.txt
# 脱敏映射表（原词→占位名）：同词表性质，属「扫描/清洗工具自身」——
# 入库等于制造新的泄露副本（自指悖论），故不入库。
rp-workspace/scripts/publish-hygiene-map.txt

# 【T-25b】M7 抽样清单（SID = 本机设备会话标识，不入库）
rp-workspace/.m7-cards.json

# 【T-25b】第三方库打包产物（构建期由 scripts/build-rp-ui.mjs 生成，可从 npm 重建）
#   jQuery / jQuery-UI / lodash / yaml / zod 的 iife 源码字符串 —— 共 1.47MB
#   许可见 docs/THIRD_PARTY_LICENSES.md（MIT/ISC）；产物不必入库。
*.gen.txt

# 【T-25b 2026-09-19】原生库（**第三方二进制**，约 100MB / 14 个 .so）
#   来源：Termux 官方源 deb（libnode=MIT · libproot/libbusybox=GPLv2 · libcares/libffi=MIT
#         · libc++_shared=Apache-2.0 WITH LLVM-exception · libsqlite3=Public Domain）
#   获取：node rp-workspace/scripts/fetch-native-libs.mjs（带 SHA256 校验，可完整重建）
#   ★ 不入库的理由：本仓库**不分发**任何第三方二进制 ⇒ 不承担 GPL 的再分发义务
#     ⇒ 本项目的 MIT 许可不受影响（法律说明见 docs/THIRD_PARTY_LICENSES.md）
rp-workspace/android/app/src/main/jniLibs/**/*.so
