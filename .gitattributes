# 行尾与编码的版本控制级约束（体检 2026-09-26 · P1-1，根因见 docs/HEALTHCHECK-D-ASSET-DRIFT-2026-09-26.md H1）
#
# ## 为什么必须有（历史复发 6+ 次的第①类断链的根因）
# 本仓无此文件 + `core.autocrlf=true` ⇒ 每次检出把全部文本文件工作区改写成 CRLF：
#   · 体检当天实测 316 个受控文件被静默改写（MainActivity.kt 工作区 2521 个 CR vs HEAD 0 个）；
#   · .ps1 的 BOM 判定依赖字节形态：双 BOM = PowerShell 解析器 2 处
#     "assignment expression is not valid"（MEMORY.md 断链①，2026-09-26 当天又复现一次）；
#   · 62 个 audit-* 闸门此前没有一个管 BOM/行尾 —— 本文件 + BOM 硬闸补上这一层。
#
# ## 为什么全仓统一 LF（而不是 .ps1 用 CRLF）
# ① 与 HEAD 现状一致（git show HEAD:build-dsht.ps1 = 单 BOM + 0 个 CR）⇒ renormalize 零 diff；
# ② pwsh 7（本仓构建链）对 LF 的 .ps1 完全兼容，从未因 LF 失败；
# ③ 混合策略（.ps1=CRLF）会在 autocrlf=true 的机器上再引入一层转换面 —— 与"减少转换"背道而驰。
#
# ## renormalize 提醒
# 首次提交本文件后跑 `git add --renormalize .` 会把工作区 316 个文件的 CRLF 收敛回 LF。
# ⚠️ 这会产生大量「仅换行」diff：必须独立提交、且按 MEMORY.md 并发协议只 add 自己的文件。

* text=auto eol=lf

# 二进制资产（防止任何行尾/编码转换）
*.png binary
*.jpg binary
*.webp binary
*.apk binary
*.zip binary
*.tar binary
*.so binary
*.jar binary
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary

# Windows 专属可执行脚本：显式 CRLF（老批处理解释器对 LF 敏感；本仓暂无 .bat，规则先行）
*.bat text eol=crlf
*.cmd text eol=crlf

# shell 脚本必须 LF（bash 对 CRLF 敏感 —— CRLF 会让 shebang 行变成 '\r' 结尾而找不到解释器）
*.sh text eol=lf
