# AI 协作规则（用户 2026-09-13 明示，跨会话生效）

> 本文件由用户口述规则固化而来，面向所有在本仓库工作的 AI 会话。
> 与 `MASTER_TODO.md` 同级；改规则请先与用户确认。

## R1【goal 文本不得留"小尾巴"】

写 goal 文本时，**不得包含需要执行中途停下来找用户拍板的决策点**。

- 所有决策必须在 goal 开头的「背景 / 用户已拍板」段落**一次性给全**。
- 执行中途若发现新的决策点：**选风险最低的默认方案继续推进**，在最终交付时
  标注「此处我按 X 处理，如需改为 Y 请告知」；**不要中断 goal 去问**。
- 用户原话（2026-09-13）：
  > 「写 goal 文本的时候别再给我留这种小尾巴，有话就说有屁就放，
  >    goal 跑到一半要我拍板是想让我中断 goal 来回答你吗？」

## R2【要 goal 就给 goal，别顺手执行】

用户说「给我 goal 文本」时，**只输出 goal 文本**，不要附带执行动作。
要执行会明说。

## R3【破坏性 git 操作前必须先固化改动】

- 任何 `git filter-repo` / `git stash` / `git checkout .` / `git reset --hard`
  之前，必须确认：**工作区改动已 commit，或已复制到仓库外**。
- **不要用 `git stash` 做临时暂存**用于跨越破坏性操作 —— stash 对象会被后续
  `filter-repo` 的 repack/clean 一并清除。
- 本案实证（2026-09-13）：执行 `git filter-repo` 前用 `stash push -u` 暂存，
  pop 后 filter-repo 把工作区重置回重写后的 HEAD，**两轮未提交改动全部丢失**
  （含 25 处 UI 修复 + C6 补丁），stash 对象亦被清除、无法恢复。

## R4【全盘扫描禁令】

- 禁止全盘/跨盘扫描、禁止逐文件遍历脚本做大范围 I/O（伤盘、耗时、烧 token）。
- 搜索一律用 `rg` / `git grep` 等索引工具；范围限定在仓库内。
- 确需大范围操作时，先报预估成本（文件数 / 耗时 / I/O 量），经用户确认再执行。
- 用户原话（2026-09-13）：
  > 「嫌我的硬盘健康度掉的不够快、token 花的太少？……扫扫工作区就得了」

## R5【描述现状必须先核实】

引用任何「现状」前，现场核实（`mtime` / `git log` / 实跑命令）；
不得转述记忆文件或二手记录。

## R6【报告精简】

单次回复不堆砌过程；长任务只在完成时汇报一次；结论先行。
